import { ClientHistory } from "@/components/features/ClientHistory";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <ClientHistory />;
};

export default page;
